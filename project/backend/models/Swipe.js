class Swipe {
  constructor({ id, userId, internshipId, direction }) {
    this.id = id;
    this.userId = userId;
    this.internshipId = internshipId;
    this.direction = direction;
  }
}

module.exports = Swipe;
